# Menu Layout Documentation Path

The former `modern-dialog/layout.md` actually documented the Dialog `Title` key. It is now named [`title.md`](title.md).

For inventory slot layouts, read [Container Layout](../container/layout.md).
